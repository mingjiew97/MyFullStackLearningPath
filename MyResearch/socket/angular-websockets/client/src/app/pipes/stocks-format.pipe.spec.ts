import { StocksFormatPipe } from './stocks-format.pipe';

describe('StocksFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new StocksFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
